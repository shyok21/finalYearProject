<!DOCTYPE html>
<html>

<head>
    <link rel="stylesheet" href="css/prcs.css">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Slab:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>

<body>
    <img src="images/header.png" style="width:100%;height:auto;position:fixed;top:0; z-index: 0;opacity: 0.9;">
    <div class="main-content">
        <%- include('../commons/header.ejs', {name: name}) %>
        <div class="menu-class">
        <%- include('prcMenu.ejs', {active: 4}) %>
        <form method="POST" action="/prcTitleChangeSubmit" class="form-class">
            <div class="dets1">
                <label for="stud_id">Student Name:</label>
                <select name="stud_id" >
                    <% results.forEach(function(result) { %>
                    <option value = <%= result.stud_id %> ><%= result.name %></option>
                    <% }); %>
                </select>
            </div>
            <div class="dets1">
                <label for="new_title">Enter new title:</label>
                <input name="new_title" required>
            </div>
            <div class="dets2">
                <input type="submit" name="submit" value="Submit Title">
            </div>
        </form>
        </div>
    </div>
</body>

</html>